// @ts-nocheck
/**
 * Generated by orval v8.5.1 🍺
 * Do not edit manually.
 * Twenty Api
 * Use this page to explore and call the **REST API**.

## Authentication

Send a Bearer token with each request:

```http
Authorization: Bearer <token>
```

Example cURL:

```bash
curl -H 'Authorization: Bearer <token>' <server>/rest/core/companies
```

Tokens can be generated in Settings → Playground and are workspace-scoped.


## Filters

Use the `filter` query parameter to narrow results.

- Format: `field[COMPARATOR]:value`
- Multiple conditions: `field1[eq]:1,field2[gte]:10` (root conjunction is AND)
- Composite fields: `field.subField[COMPARATOR]:value`
- Common comparators: `eq`, `neq`, `in`, `containsAny`, `is`, `gt`, `gte`, `lt`, `lte`, `startsWith`, `like`, `ilike`
- Wildcards: For `like`/`ilike`, use `%` as a wildcard (e.g. `%value%` for substring match)

Examples:

```text
filter=status[eq]:"open"
filter=createdAt[gte]:"2024-01-01"
filter=owner.name[ilike]:"%smith%"
filter=id[in]:["id-1","id-2"]
filter=deletedAt[is]:NULL
filter=isActive[eq]:true
```

Advanced (optional): `and(...)`, `or(...)`, `not(...)` (`not` wraps one condition)

```text
filter=or(status[eq]:"open",assigneeId[is]:NULL)
```

Notes: Strings and dates are quoted; numbers are not.

## Pagination and ordering

All list endpoints use cursor-based pagination.

- Use **limit** to cap page size (default: 60, max: 60).
- Use **starting_after** to fetch the next page (forward).
- Use **ending_before** to fetch the previous page (backward).
- Responses include **pageInfo** with `hasNextPage`, `startCursor`, and `endCursor`.

Examples:

```bash
# First page
curl -H 'Authorization: Bearer <token>' \
  '<server>/rest/core/companies?limit=60'

# Next page
curl -H 'Authorization: Bearer <token>' \
  '<server>/rest/core/companies?limit=60&starting_after=<endCursorFromPreviousPage>'

# Previous page
curl -H 'Authorization: Bearer <token>' \
  '<server>/rest/core/companies?limit=60&ending_before=<startCursorFromCurrentPage>'
```

You can combine pagination with filters and ordering.

Ordering with `order_by`:
- Shape: `field1,field2[DIRECTION2]`
- Directions: AscNullsFirst, AscNullsLast, DescNullsFirst, DescNullsLast
- Default per-field direction: AscNullsFirst

Examples:
```text
order_by=createdAt
order_by=id[AscNullsFirst],createdAt[DescNullsLast]
```

## Usage with LLMs

You can use AI to generate code based on the OpenAPI schema with the following URLs:

```text
Core: https://twenty.mardu.systems/rest/open-api/core?token=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..tItC59wNx5x6jeF3jAm2405NxIUBhDMjYCIHlX-WYm0
Metadata: https://twenty.mardu.systems/rest/open-api/metadata?token=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..tItC59wNx5x6jeF3jAm2405NxIUBhDMjYCIHlX-WYm0
```

Quick prompt example (Cursor or any agent):

```text
Here is an OpenAPI schema for the Twenty REST API:
https://twenty.mardu.systems/rest/open-api/core?token=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..tItC59wNx5x6jeF3jAm2405NxIUBhDMjYCIHlX-WYm0

Use it to list companies created after 2024-01-01, ordered by createdAt desc, and include only 20 results.
```

Notes:
- Treat the token like a secret; prefer a short-lived Playground token.
- Most editors can fetch and process the schema even if it's large.

 * OpenAPI spec version: v0.1
 */
import type {
  CreateManyPeople201,
  CreateManyPeopleParams,
  CreateOnePerson201,
  CreateOnePersonParams,
  DeleteManyPeople200,
  DeleteManyPeopleParams,
  DeleteOnePerson200,
  DeleteOnePersonParams,
  FindManyPeople200,
  FindManyPeopleParams,
  FindOnePerson200,
  FindOnePersonParams,
  FindPersonDuplicates200,
  FindPersonDuplicatesBody,
  FindPersonDuplicatesParams,
  GroupByPeople200,
  GroupByPeopleParams,
  MergeManyPeople200,
  MergeManyPeopleBody,
  MergeManyPeopleParams,
  Person,
  PersonForUpdate,
  RestoreManyPeople200,
  RestoreManyPeopleParams,
  RestoreOnePerson200,
  RestoreOnePersonParams,
  UpdateManyPeople200,
  UpdateManyPeopleParams,
  UpdateOnePerson200,
  UpdateOnePersonParams,
} from '../../model';

/**
 * **order_by**, **filter**, **limit**, **depth**, **starting_after** or **ending_before** can be provided to request your **people**
 * @summary Find Many people
 */
export const getFindManyPeopleUrl = (params?: FindManyPeopleParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/people?${stringifiedParams}` : `/people`;
};

export const findManyPeople = async (
  params?: FindManyPeopleParams,
  options?: RequestInit,
): Promise<FindManyPeople200> => {
  const res = await fetch(getFindManyPeopleUrl(params), {
    ...options,
    method: 'GET',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: FindManyPeople200 = body ? JSON.parse(body) : {};
  return data;
};

/**
 * @summary Create One person
 */
export const getCreateOnePersonUrl = (params?: CreateOnePersonParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/people?${stringifiedParams}` : `/people`;
};

export const createOnePerson = async (
  person: Person,
  params?: CreateOnePersonParams,
  options?: RequestInit,
): Promise<CreateOnePerson201> => {
  const res = await fetch(getCreateOnePersonUrl(params), {
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(person),
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: CreateOnePerson201 = body ? JSON.parse(body) : {};
  return data;
};

/**
 * @summary Delete Many people
 */
export const getDeleteManyPeopleUrl = (params?: DeleteManyPeopleParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/people?${stringifiedParams}` : `/people`;
};

export const deleteManyPeople = async (
  params?: DeleteManyPeopleParams,
  options?: RequestInit,
): Promise<DeleteManyPeople200> => {
  const res = await fetch(getDeleteManyPeopleUrl(params), {
    ...options,
    method: 'DELETE',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: DeleteManyPeople200 = body ? JSON.parse(body) : {};
  return data;
};

/**
 * @summary Update Many people
 */
export const getUpdateManyPeopleUrl = (params?: UpdateManyPeopleParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/people?${stringifiedParams}` : `/people`;
};

export const updateManyPeople = async (
  personForUpdate: PersonForUpdate,
  params?: UpdateManyPeopleParams,
  options?: RequestInit,
): Promise<UpdateManyPeople200> => {
  const res = await fetch(getUpdateManyPeopleUrl(params), {
    ...options,
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(personForUpdate),
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: UpdateManyPeople200 = body ? JSON.parse(body) : {};
  return data;
};

/**
 * @summary Create Many people
 */
export const getCreateManyPeopleUrl = (params?: CreateManyPeopleParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/batch/people?${stringifiedParams}` : `/batch/people`;
};

export const createManyPeople = async (
  person: Person[],
  params?: CreateManyPeopleParams,
  options?: RequestInit,
): Promise<CreateManyPeople201> => {
  const res = await fetch(getCreateManyPeopleUrl(params), {
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(person),
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: CreateManyPeople201 = body ? JSON.parse(body) : {};
  return data;
};

/**
 * **depth** can be provided to request your **person**
 * @summary Find One person
 */
export const getFindOnePersonUrl = (id: string, params?: FindOnePersonParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/people/${id}?${stringifiedParams}` : `/people/${id}`;
};

export const findOnePerson = async (
  id: string,
  params?: FindOnePersonParams,
  options?: RequestInit,
): Promise<FindOnePerson200> => {
  const res = await fetch(getFindOnePersonUrl(id, params), {
    ...options,
    method: 'GET',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: FindOnePerson200 = body ? JSON.parse(body) : {};
  return data;
};

/**
 * @summary Delete One person
 */
export const getDeleteOnePersonUrl = (id: string, params?: DeleteOnePersonParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/people/${id}?${stringifiedParams}` : `/people/${id}`;
};

export const deleteOnePerson = async (
  id: string,
  params?: DeleteOnePersonParams,
  options?: RequestInit,
): Promise<DeleteOnePerson200> => {
  const res = await fetch(getDeleteOnePersonUrl(id, params), {
    ...options,
    method: 'DELETE',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: DeleteOnePerson200 = body ? JSON.parse(body) : {};
  return data;
};

/**
 * @summary Update One person
 */
export const getUpdateOnePersonUrl = (id: string, params?: UpdateOnePersonParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/people/${id}?${stringifiedParams}` : `/people/${id}`;
};

export const updateOnePerson = async (
  id: string,
  personForUpdate: PersonForUpdate,
  params?: UpdateOnePersonParams,
  options?: RequestInit,
): Promise<UpdateOnePerson200> => {
  const res = await fetch(getUpdateOnePersonUrl(id, params), {
    ...options,
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(personForUpdate),
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: UpdateOnePerson200 = body ? JSON.parse(body) : {};
  return data;
};

/**
 * **depth** can be provided to request your **person**
 * @summary Find person Duplicates
 */
export const getFindPersonDuplicatesUrl = (params?: FindPersonDuplicatesParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/people/duplicates?${stringifiedParams}`
    : `/people/duplicates`;
};

export const findPersonDuplicates = async (
  findPersonDuplicatesBody: FindPersonDuplicatesBody,
  params?: FindPersonDuplicatesParams,
  options?: RequestInit,
): Promise<FindPersonDuplicates200> => {
  const res = await fetch(getFindPersonDuplicatesUrl(params), {
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(findPersonDuplicatesBody),
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: FindPersonDuplicates200 = body ? JSON.parse(body) : {};
  return data;
};

/**
 * @summary Restore One person
 */
export const getRestoreOnePersonUrl = (id: string, params?: RestoreOnePersonParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/restore/people/${id}?${stringifiedParams}`
    : `/restore/people/${id}`;
};

export const restoreOnePerson = async (
  id: string,
  params?: RestoreOnePersonParams,
  options?: RequestInit,
): Promise<RestoreOnePerson200> => {
  const res = await fetch(getRestoreOnePersonUrl(id, params), {
    ...options,
    method: 'PATCH',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: RestoreOnePerson200 = body ? JSON.parse(body) : {};
  return data;
};

/**
 * @summary Restore Many people
 */
export const getRestoreManyPeopleUrl = (params?: RestoreManyPeopleParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/restore/people?${stringifiedParams}` : `/restore/people`;
};

export const restoreManyPeople = async (
  params?: RestoreManyPeopleParams,
  options?: RequestInit,
): Promise<RestoreManyPeople200> => {
  const res = await fetch(getRestoreManyPeopleUrl(params), {
    ...options,
    method: 'PATCH',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: RestoreManyPeople200 = body ? JSON.parse(body) : {};
  return data;
};

/**
 * @summary Merge Many people
 */
export const getMergeManyPeopleUrl = (params?: MergeManyPeopleParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/people/merge?${stringifiedParams}` : `/people/merge`;
};

export const mergeManyPeople = async (
  mergeManyPeopleBody: MergeManyPeopleBody,
  params?: MergeManyPeopleParams,
  options?: RequestInit,
): Promise<MergeManyPeople200> => {
  const res = await fetch(getMergeManyPeopleUrl(params), {
    ...options,
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(mergeManyPeopleBody),
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: MergeManyPeople200 = body ? JSON.parse(body) : {};
  return data;
};

/**
 * Groups **people** by specified fields and optionally computes aggregate values for each group.
 * @summary Group By people
 */
export const getGroupByPeopleUrl = (params: GroupByPeopleParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/people/groupBy?${stringifiedParams}` : `/people/groupBy`;
};

export const groupByPeople = async (
  params: GroupByPeopleParams,
  options?: RequestInit,
): Promise<GroupByPeople200> => {
  const res = await fetch(getGroupByPeopleUrl(params), {
    ...options,
    method: 'GET',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: GroupByPeople200 = body ? JSON.parse(body) : {};
  return data;
};
