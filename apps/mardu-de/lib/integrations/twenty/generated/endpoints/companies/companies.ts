// @ts-nocheck
/**
 * Generated by orval v8.5.1 🍺
 * Do not edit manually.
 * Twenty Api
 * Use this page to explore and call the **REST API**.

## Authentication

Send a Bearer token with each request:

```http
Authorization: Bearer <token>
```

Example cURL:

```bash
curl -H 'Authorization: Bearer <token>' <server>/rest/core/companies
```

Tokens can be generated in Settings → Playground and are workspace-scoped.


## Filters

Use the `filter` query parameter to narrow results.

- Format: `field[COMPARATOR]:value`
- Multiple conditions: `field1[eq]:1,field2[gte]:10` (root conjunction is AND)
- Composite fields: `field.subField[COMPARATOR]:value`
- Common comparators: `eq`, `neq`, `in`, `containsAny`, `is`, `gt`, `gte`, `lt`, `lte`, `startsWith`, `like`, `ilike`
- Wildcards: For `like`/`ilike`, use `%` as a wildcard (e.g. `%value%` for substring match)

Examples:

```text
filter=status[eq]:"open"
filter=createdAt[gte]:"2024-01-01"
filter=owner.name[ilike]:"%smith%"
filter=id[in]:["id-1","id-2"]
filter=deletedAt[is]:NULL
filter=isActive[eq]:true
```

Advanced (optional): `and(...)`, `or(...)`, `not(...)` (`not` wraps one condition)

```text
filter=or(status[eq]:"open",assigneeId[is]:NULL)
```

Notes: Strings and dates are quoted; numbers are not.

## Pagination and ordering

All list endpoints use cursor-based pagination.

- Use **limit** to cap page size (default: 60, max: 60).
- Use **starting_after** to fetch the next page (forward).
- Use **ending_before** to fetch the previous page (backward).
- Responses include **pageInfo** with `hasNextPage`, `startCursor`, and `endCursor`.

Examples:

```bash
# First page
curl -H 'Authorization: Bearer <token>' \
  '<server>/rest/core/companies?limit=60'

# Next page
curl -H 'Authorization: Bearer <token>' \
  '<server>/rest/core/companies?limit=60&starting_after=<endCursorFromPreviousPage>'

# Previous page
curl -H 'Authorization: Bearer <token>' \
  '<server>/rest/core/companies?limit=60&ending_before=<startCursorFromCurrentPage>'
```

You can combine pagination with filters and ordering.

Ordering with `order_by`:
- Shape: `field1,field2[DIRECTION2]`
- Directions: AscNullsFirst, AscNullsLast, DescNullsFirst, DescNullsLast
- Default per-field direction: AscNullsFirst

Examples:
```text
order_by=createdAt
order_by=id[AscNullsFirst],createdAt[DescNullsLast]
```

## Usage with LLMs

You can use AI to generate code based on the OpenAPI schema with the following URLs:

```text
Core: https://twenty.mardu.systems/rest/open-api/core?token=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..tItC59wNx5x6jeF3jAm2405NxIUBhDMjYCIHlX-WYm0
Metadata: https://twenty.mardu.systems/rest/open-api/metadata?token=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..tItC59wNx5x6jeF3jAm2405NxIUBhDMjYCIHlX-WYm0
```

Quick prompt example (Cursor or any agent):

```text
Here is an OpenAPI schema for the Twenty REST API:
https://twenty.mardu.systems/rest/open-api/core?token=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..tItC59wNx5x6jeF3jAm2405NxIUBhDMjYCIHlX-WYm0

Use it to list companies created after 2024-01-01, ordered by createdAt desc, and include only 20 results.
```

Notes:
- Treat the token like a secret; prefer a short-lived Playground token.
- Most editors can fetch and process the schema even if it's large.

 * OpenAPI spec version: v0.1
 */
import type {
  Company,
  CompanyForUpdate,
  CreateManyCompanies201,
  CreateManyCompaniesParams,
  CreateOneCompany201,
  CreateOneCompanyParams,
  DeleteManyCompanies200,
  DeleteManyCompaniesParams,
  DeleteOneCompany200,
  DeleteOneCompanyParams,
  FindCompanyDuplicates200,
  FindCompanyDuplicatesBody,
  FindCompanyDuplicatesParams,
  FindManyCompanies200,
  FindManyCompaniesParams,
  FindOneCompany200,
  FindOneCompanyParams,
  GroupByCompanies200,
  GroupByCompaniesParams,
  MergeManyCompanies200,
  MergeManyCompaniesBody,
  MergeManyCompaniesParams,
  RestoreManyCompanies200,
  RestoreManyCompaniesParams,
  RestoreOneCompany200,
  RestoreOneCompanyParams,
  UpdateManyCompanies200,
  UpdateManyCompaniesParams,
  UpdateOneCompany200,
  UpdateOneCompanyParams,
} from '../../model';

/**
 * **order_by**, **filter**, **limit**, **depth**, **starting_after** or **ending_before** can be provided to request your **companies**
 * @summary Find Many companies
 */
export const getFindManyCompaniesUrl = (params?: FindManyCompaniesParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/companies?${stringifiedParams}` : `/companies`;
};

export const findManyCompanies = async (
  params?: FindManyCompaniesParams,
  options?: RequestInit,
): Promise<FindManyCompanies200> => {
  const res = await fetch(getFindManyCompaniesUrl(params), {
    ...options,
    method: 'GET',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: FindManyCompanies200 = body ? JSON.parse(body) : {};
  return data;
};

/**
 * @summary Create One company
 */
export const getCreateOneCompanyUrl = (params?: CreateOneCompanyParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/companies?${stringifiedParams}` : `/companies`;
};

export const createOneCompany = async (
  company: Company,
  params?: CreateOneCompanyParams,
  options?: RequestInit,
): Promise<CreateOneCompany201> => {
  const res = await fetch(getCreateOneCompanyUrl(params), {
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(company),
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: CreateOneCompany201 = body ? JSON.parse(body) : {};
  return data;
};

/**
 * @summary Delete Many companies
 */
export const getDeleteManyCompaniesUrl = (params?: DeleteManyCompaniesParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/companies?${stringifiedParams}` : `/companies`;
};

export const deleteManyCompanies = async (
  params?: DeleteManyCompaniesParams,
  options?: RequestInit,
): Promise<DeleteManyCompanies200> => {
  const res = await fetch(getDeleteManyCompaniesUrl(params), {
    ...options,
    method: 'DELETE',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: DeleteManyCompanies200 = body ? JSON.parse(body) : {};
  return data;
};

/**
 * @summary Update Many companies
 */
export const getUpdateManyCompaniesUrl = (params?: UpdateManyCompaniesParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/companies?${stringifiedParams}` : `/companies`;
};

export const updateManyCompanies = async (
  companyForUpdate: CompanyForUpdate,
  params?: UpdateManyCompaniesParams,
  options?: RequestInit,
): Promise<UpdateManyCompanies200> => {
  const res = await fetch(getUpdateManyCompaniesUrl(params), {
    ...options,
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(companyForUpdate),
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: UpdateManyCompanies200 = body ? JSON.parse(body) : {};
  return data;
};

/**
 * @summary Create Many companies
 */
export const getCreateManyCompaniesUrl = (params?: CreateManyCompaniesParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/batch/companies?${stringifiedParams}`
    : `/batch/companies`;
};

export const createManyCompanies = async (
  company: Company[],
  params?: CreateManyCompaniesParams,
  options?: RequestInit,
): Promise<CreateManyCompanies201> => {
  const res = await fetch(getCreateManyCompaniesUrl(params), {
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(company),
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: CreateManyCompanies201 = body ? JSON.parse(body) : {};
  return data;
};

/**
 * **depth** can be provided to request your **company**
 * @summary Find One company
 */
export const getFindOneCompanyUrl = (id: string, params?: FindOneCompanyParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/companies/${id}?${stringifiedParams}`
    : `/companies/${id}`;
};

export const findOneCompany = async (
  id: string,
  params?: FindOneCompanyParams,
  options?: RequestInit,
): Promise<FindOneCompany200> => {
  const res = await fetch(getFindOneCompanyUrl(id, params), {
    ...options,
    method: 'GET',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: FindOneCompany200 = body ? JSON.parse(body) : {};
  return data;
};

/**
 * @summary Delete One company
 */
export const getDeleteOneCompanyUrl = (id: string, params?: DeleteOneCompanyParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/companies/${id}?${stringifiedParams}`
    : `/companies/${id}`;
};

export const deleteOneCompany = async (
  id: string,
  params?: DeleteOneCompanyParams,
  options?: RequestInit,
): Promise<DeleteOneCompany200> => {
  const res = await fetch(getDeleteOneCompanyUrl(id, params), {
    ...options,
    method: 'DELETE',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: DeleteOneCompany200 = body ? JSON.parse(body) : {};
  return data;
};

/**
 * @summary Update One company
 */
export const getUpdateOneCompanyUrl = (id: string, params?: UpdateOneCompanyParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/companies/${id}?${stringifiedParams}`
    : `/companies/${id}`;
};

export const updateOneCompany = async (
  id: string,
  companyForUpdate: CompanyForUpdate,
  params?: UpdateOneCompanyParams,
  options?: RequestInit,
): Promise<UpdateOneCompany200> => {
  const res = await fetch(getUpdateOneCompanyUrl(id, params), {
    ...options,
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(companyForUpdate),
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: UpdateOneCompany200 = body ? JSON.parse(body) : {};
  return data;
};

/**
 * **depth** can be provided to request your **company**
 * @summary Find company Duplicates
 */
export const getFindCompanyDuplicatesUrl = (params?: FindCompanyDuplicatesParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/companies/duplicates?${stringifiedParams}`
    : `/companies/duplicates`;
};

export const findCompanyDuplicates = async (
  findCompanyDuplicatesBody: FindCompanyDuplicatesBody,
  params?: FindCompanyDuplicatesParams,
  options?: RequestInit,
): Promise<FindCompanyDuplicates200> => {
  const res = await fetch(getFindCompanyDuplicatesUrl(params), {
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(findCompanyDuplicatesBody),
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: FindCompanyDuplicates200 = body ? JSON.parse(body) : {};
  return data;
};

/**
 * @summary Restore One company
 */
export const getRestoreOneCompanyUrl = (id: string, params?: RestoreOneCompanyParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/restore/companies/${id}?${stringifiedParams}`
    : `/restore/companies/${id}`;
};

export const restoreOneCompany = async (
  id: string,
  params?: RestoreOneCompanyParams,
  options?: RequestInit,
): Promise<RestoreOneCompany200> => {
  const res = await fetch(getRestoreOneCompanyUrl(id, params), {
    ...options,
    method: 'PATCH',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: RestoreOneCompany200 = body ? JSON.parse(body) : {};
  return data;
};

/**
 * @summary Restore Many companies
 */
export const getRestoreManyCompaniesUrl = (params?: RestoreManyCompaniesParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/restore/companies?${stringifiedParams}`
    : `/restore/companies`;
};

export const restoreManyCompanies = async (
  params?: RestoreManyCompaniesParams,
  options?: RequestInit,
): Promise<RestoreManyCompanies200> => {
  const res = await fetch(getRestoreManyCompaniesUrl(params), {
    ...options,
    method: 'PATCH',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: RestoreManyCompanies200 = body ? JSON.parse(body) : {};
  return data;
};

/**
 * @summary Merge Many companies
 */
export const getMergeManyCompaniesUrl = (params?: MergeManyCompaniesParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/companies/merge?${stringifiedParams}`
    : `/companies/merge`;
};

export const mergeManyCompanies = async (
  mergeManyCompaniesBody: MergeManyCompaniesBody,
  params?: MergeManyCompaniesParams,
  options?: RequestInit,
): Promise<MergeManyCompanies200> => {
  const res = await fetch(getMergeManyCompaniesUrl(params), {
    ...options,
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(mergeManyCompaniesBody),
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: MergeManyCompanies200 = body ? JSON.parse(body) : {};
  return data;
};

/**
 * Groups **companies** by specified fields and optionally computes aggregate values for each group.
 * @summary Group By companies
 */
export const getGroupByCompaniesUrl = (params: GroupByCompaniesParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/companies/groupBy?${stringifiedParams}`
    : `/companies/groupBy`;
};

export const groupByCompanies = async (
  params: GroupByCompaniesParams,
  options?: RequestInit,
): Promise<GroupByCompanies200> => {
  const res = await fetch(getGroupByCompaniesUrl(params), {
    ...options,
    method: 'GET',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: GroupByCompanies200 = body ? JSON.parse(body) : {};
  return data;
};
