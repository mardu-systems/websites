// @ts-nocheck
/**
 * Generated by orval v8.5.1 🍺
 * Do not edit manually.
 * Twenty Api
 * Use this page to explore and call the **REST API**.

## Authentication

Send a Bearer token with each request:

```http
Authorization: Bearer <token>
```

Example cURL:

```bash
curl -H 'Authorization: Bearer <token>' <server>/rest/core/companies
```

Tokens can be generated in Settings → Playground and are workspace-scoped.


## Filters

Use the `filter` query parameter to narrow results.

- Format: `field[COMPARATOR]:value`
- Multiple conditions: `field1[eq]:1,field2[gte]:10` (root conjunction is AND)
- Composite fields: `field.subField[COMPARATOR]:value`
- Common comparators: `eq`, `neq`, `in`, `containsAny`, `is`, `gt`, `gte`, `lt`, `lte`, `startsWith`, `like`, `ilike`
- Wildcards: For `like`/`ilike`, use `%` as a wildcard (e.g. `%value%` for substring match)

Examples:

```text
filter=status[eq]:"open"
filter=createdAt[gte]:"2024-01-01"
filter=owner.name[ilike]:"%smith%"
filter=id[in]:["id-1","id-2"]
filter=deletedAt[is]:NULL
filter=isActive[eq]:true
```

Advanced (optional): `and(...)`, `or(...)`, `not(...)` (`not` wraps one condition)

```text
filter=or(status[eq]:"open",assigneeId[is]:NULL)
```

Notes: Strings and dates are quoted; numbers are not.

## Pagination and ordering

All list endpoints use cursor-based pagination.

- Use **limit** to cap page size (default: 60, max: 60).
- Use **starting_after** to fetch the next page (forward).
- Use **ending_before** to fetch the previous page (backward).
- Responses include **pageInfo** with `hasNextPage`, `startCursor`, and `endCursor`.

Examples:

```bash
# First page
curl -H 'Authorization: Bearer <token>' \
  '<server>/rest/core/companies?limit=60'

# Next page
curl -H 'Authorization: Bearer <token>' \
  '<server>/rest/core/companies?limit=60&starting_after=<endCursorFromPreviousPage>'

# Previous page
curl -H 'Authorization: Bearer <token>' \
  '<server>/rest/core/companies?limit=60&ending_before=<startCursorFromCurrentPage>'
```

You can combine pagination with filters and ordering.

Ordering with `order_by`:
- Shape: `field1,field2[DIRECTION2]`
- Directions: AscNullsFirst, AscNullsLast, DescNullsFirst, DescNullsLast
- Default per-field direction: AscNullsFirst

Examples:
```text
order_by=createdAt
order_by=id[AscNullsFirst],createdAt[DescNullsLast]
```

## Usage with LLMs

You can use AI to generate code based on the OpenAPI schema with the following URLs:

```text
Core: https://twenty.mardu.systems/rest/open-api/core?token=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..tItC59wNx5x6jeF3jAm2405NxIUBhDMjYCIHlX-WYm0
Metadata: https://twenty.mardu.systems/rest/open-api/metadata?token=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..tItC59wNx5x6jeF3jAm2405NxIUBhDMjYCIHlX-WYm0
```

Quick prompt example (Cursor or any agent):

```text
Here is an OpenAPI schema for the Twenty REST API:
https://twenty.mardu.systems/rest/open-api/core?token=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..tItC59wNx5x6jeF3jAm2405NxIUBhDMjYCIHlX-WYm0

Use it to list companies created after 2024-01-01, ordered by createdAt desc, and include only 20 results.
```

Notes:
- Treat the token like a secret; prefer a short-lived Playground token.
- Most editors can fetch and process the schema even if it's large.

 * OpenAPI spec version: v0.1
 */
import type {
  CreateManyNoteTargets201,
  CreateManyNoteTargetsParams,
  CreateOneNoteTarget201,
  CreateOneNoteTargetParams,
  DeleteManyNoteTargets200,
  DeleteManyNoteTargetsParams,
  DeleteOneNoteTarget200,
  DeleteOneNoteTargetParams,
  FindManyNoteTargets200,
  FindManyNoteTargetsParams,
  FindNoteTargetDuplicates200,
  FindNoteTargetDuplicatesBody,
  FindNoteTargetDuplicatesParams,
  FindOneNoteTarget200,
  FindOneNoteTargetParams,
  GroupByNoteTargets200,
  GroupByNoteTargetsParams,
  MergeManyNoteTargets200,
  MergeManyNoteTargetsBody,
  MergeManyNoteTargetsParams,
  NoteTarget,
  NoteTargetForUpdate,
  RestoreManyNoteTargets200,
  RestoreManyNoteTargetsParams,
  RestoreOneNoteTarget200,
  RestoreOneNoteTargetParams,
  UpdateManyNoteTargets200,
  UpdateManyNoteTargetsParams,
  UpdateOneNoteTarget200,
  UpdateOneNoteTargetParams,
} from '../../model';

/**
 * **order_by**, **filter**, **limit**, **depth**, **starting_after** or **ending_before** can be provided to request your **noteTargets**
 * @summary Find Many noteTargets
 */
export const getFindManyNoteTargetsUrl = (params?: FindManyNoteTargetsParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/noteTargets?${stringifiedParams}` : `/noteTargets`;
};

export const findManyNoteTargets = async (
  params?: FindManyNoteTargetsParams,
  options?: RequestInit,
): Promise<FindManyNoteTargets200> => {
  const res = await fetch(getFindManyNoteTargetsUrl(params), {
    ...options,
    method: 'GET',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: FindManyNoteTargets200 = body ? JSON.parse(body) : {};
  return data;
};

/**
 * @summary Create One noteTarget
 */
export const getCreateOneNoteTargetUrl = (params?: CreateOneNoteTargetParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/noteTargets?${stringifiedParams}` : `/noteTargets`;
};

export const createOneNoteTarget = async (
  noteTarget: NoteTarget,
  params?: CreateOneNoteTargetParams,
  options?: RequestInit,
): Promise<CreateOneNoteTarget201> => {
  const res = await fetch(getCreateOneNoteTargetUrl(params), {
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(noteTarget),
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: CreateOneNoteTarget201 = body ? JSON.parse(body) : {};
  return data;
};

/**
 * @summary Delete Many noteTargets
 */
export const getDeleteManyNoteTargetsUrl = (params?: DeleteManyNoteTargetsParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/noteTargets?${stringifiedParams}` : `/noteTargets`;
};

export const deleteManyNoteTargets = async (
  params?: DeleteManyNoteTargetsParams,
  options?: RequestInit,
): Promise<DeleteManyNoteTargets200> => {
  const res = await fetch(getDeleteManyNoteTargetsUrl(params), {
    ...options,
    method: 'DELETE',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: DeleteManyNoteTargets200 = body ? JSON.parse(body) : {};
  return data;
};

/**
 * @summary Update Many noteTargets
 */
export const getUpdateManyNoteTargetsUrl = (params?: UpdateManyNoteTargetsParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/noteTargets?${stringifiedParams}` : `/noteTargets`;
};

export const updateManyNoteTargets = async (
  noteTargetForUpdate: NoteTargetForUpdate,
  params?: UpdateManyNoteTargetsParams,
  options?: RequestInit,
): Promise<UpdateManyNoteTargets200> => {
  const res = await fetch(getUpdateManyNoteTargetsUrl(params), {
    ...options,
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(noteTargetForUpdate),
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: UpdateManyNoteTargets200 = body ? JSON.parse(body) : {};
  return data;
};

/**
 * @summary Create Many noteTargets
 */
export const getCreateManyNoteTargetsUrl = (params?: CreateManyNoteTargetsParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/batch/noteTargets?${stringifiedParams}`
    : `/batch/noteTargets`;
};

export const createManyNoteTargets = async (
  noteTarget: NoteTarget[],
  params?: CreateManyNoteTargetsParams,
  options?: RequestInit,
): Promise<CreateManyNoteTargets201> => {
  const res = await fetch(getCreateManyNoteTargetsUrl(params), {
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(noteTarget),
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: CreateManyNoteTargets201 = body ? JSON.parse(body) : {};
  return data;
};

/**
 * **depth** can be provided to request your **noteTarget**
 * @summary Find One noteTarget
 */
export const getFindOneNoteTargetUrl = (id: string, params?: FindOneNoteTargetParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/noteTargets/${id}?${stringifiedParams}`
    : `/noteTargets/${id}`;
};

export const findOneNoteTarget = async (
  id: string,
  params?: FindOneNoteTargetParams,
  options?: RequestInit,
): Promise<FindOneNoteTarget200> => {
  const res = await fetch(getFindOneNoteTargetUrl(id, params), {
    ...options,
    method: 'GET',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: FindOneNoteTarget200 = body ? JSON.parse(body) : {};
  return data;
};

/**
 * @summary Delete One noteTarget
 */
export const getDeleteOneNoteTargetUrl = (id: string, params?: DeleteOneNoteTargetParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/noteTargets/${id}?${stringifiedParams}`
    : `/noteTargets/${id}`;
};

export const deleteOneNoteTarget = async (
  id: string,
  params?: DeleteOneNoteTargetParams,
  options?: RequestInit,
): Promise<DeleteOneNoteTarget200> => {
  const res = await fetch(getDeleteOneNoteTargetUrl(id, params), {
    ...options,
    method: 'DELETE',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: DeleteOneNoteTarget200 = body ? JSON.parse(body) : {};
  return data;
};

/**
 * @summary Update One noteTarget
 */
export const getUpdateOneNoteTargetUrl = (id: string, params?: UpdateOneNoteTargetParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/noteTargets/${id}?${stringifiedParams}`
    : `/noteTargets/${id}`;
};

export const updateOneNoteTarget = async (
  id: string,
  noteTargetForUpdate: NoteTargetForUpdate,
  params?: UpdateOneNoteTargetParams,
  options?: RequestInit,
): Promise<UpdateOneNoteTarget200> => {
  const res = await fetch(getUpdateOneNoteTargetUrl(id, params), {
    ...options,
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(noteTargetForUpdate),
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: UpdateOneNoteTarget200 = body ? JSON.parse(body) : {};
  return data;
};

/**
 * **depth** can be provided to request your **noteTarget**
 * @summary Find noteTarget Duplicates
 */
export const getFindNoteTargetDuplicatesUrl = (params?: FindNoteTargetDuplicatesParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/noteTargets/duplicates?${stringifiedParams}`
    : `/noteTargets/duplicates`;
};

export const findNoteTargetDuplicates = async (
  findNoteTargetDuplicatesBody: FindNoteTargetDuplicatesBody,
  params?: FindNoteTargetDuplicatesParams,
  options?: RequestInit,
): Promise<FindNoteTargetDuplicates200> => {
  const res = await fetch(getFindNoteTargetDuplicatesUrl(params), {
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(findNoteTargetDuplicatesBody),
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: FindNoteTargetDuplicates200 = body ? JSON.parse(body) : {};
  return data;
};

/**
 * @summary Restore One noteTarget
 */
export const getRestoreOneNoteTargetUrl = (id: string, params?: RestoreOneNoteTargetParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/restore/noteTargets/${id}?${stringifiedParams}`
    : `/restore/noteTargets/${id}`;
};

export const restoreOneNoteTarget = async (
  id: string,
  params?: RestoreOneNoteTargetParams,
  options?: RequestInit,
): Promise<RestoreOneNoteTarget200> => {
  const res = await fetch(getRestoreOneNoteTargetUrl(id, params), {
    ...options,
    method: 'PATCH',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: RestoreOneNoteTarget200 = body ? JSON.parse(body) : {};
  return data;
};

/**
 * @summary Restore Many noteTargets
 */
export const getRestoreManyNoteTargetsUrl = (params?: RestoreManyNoteTargetsParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/restore/noteTargets?${stringifiedParams}`
    : `/restore/noteTargets`;
};

export const restoreManyNoteTargets = async (
  params?: RestoreManyNoteTargetsParams,
  options?: RequestInit,
): Promise<RestoreManyNoteTargets200> => {
  const res = await fetch(getRestoreManyNoteTargetsUrl(params), {
    ...options,
    method: 'PATCH',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: RestoreManyNoteTargets200 = body ? JSON.parse(body) : {};
  return data;
};

/**
 * @summary Merge Many noteTargets
 */
export const getMergeManyNoteTargetsUrl = (params?: MergeManyNoteTargetsParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/noteTargets/merge?${stringifiedParams}`
    : `/noteTargets/merge`;
};

export const mergeManyNoteTargets = async (
  mergeManyNoteTargetsBody: MergeManyNoteTargetsBody,
  params?: MergeManyNoteTargetsParams,
  options?: RequestInit,
): Promise<MergeManyNoteTargets200> => {
  const res = await fetch(getMergeManyNoteTargetsUrl(params), {
    ...options,
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(mergeManyNoteTargetsBody),
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: MergeManyNoteTargets200 = body ? JSON.parse(body) : {};
  return data;
};

/**
 * Groups **noteTargets** by specified fields and optionally computes aggregate values for each group.
 * @summary Group By noteTargets
 */
export const getGroupByNoteTargetsUrl = (params: GroupByNoteTargetsParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/noteTargets/groupBy?${stringifiedParams}`
    : `/noteTargets/groupBy`;
};

export const groupByNoteTargets = async (
  params: GroupByNoteTargetsParams,
  options?: RequestInit,
): Promise<GroupByNoteTargets200> => {
  const res = await fetch(getGroupByNoteTargetsUrl(params), {
    ...options,
    method: 'GET',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: GroupByNoteTargets200 = body ? JSON.parse(body) : {};
  return data;
};
