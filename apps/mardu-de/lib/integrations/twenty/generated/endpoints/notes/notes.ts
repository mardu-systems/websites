// @ts-nocheck
/**
 * Generated by orval v8.5.1 🍺
 * Do not edit manually.
 * Twenty Api
 * Use this page to explore and call the **REST API**.

## Authentication

Send a Bearer token with each request:

```http
Authorization: Bearer <token>
```

Example cURL:

```bash
curl -H 'Authorization: Bearer <token>' <server>/rest/core/companies
```

Tokens can be generated in Settings → Playground and are workspace-scoped.


## Filters

Use the `filter` query parameter to narrow results.

- Format: `field[COMPARATOR]:value`
- Multiple conditions: `field1[eq]:1,field2[gte]:10` (root conjunction is AND)
- Composite fields: `field.subField[COMPARATOR]:value`
- Common comparators: `eq`, `neq`, `in`, `containsAny`, `is`, `gt`, `gte`, `lt`, `lte`, `startsWith`, `like`, `ilike`
- Wildcards: For `like`/`ilike`, use `%` as a wildcard (e.g. `%value%` for substring match)

Examples:

```text
filter=status[eq]:"open"
filter=createdAt[gte]:"2024-01-01"
filter=owner.name[ilike]:"%smith%"
filter=id[in]:["id-1","id-2"]
filter=deletedAt[is]:NULL
filter=isActive[eq]:true
```

Advanced (optional): `and(...)`, `or(...)`, `not(...)` (`not` wraps one condition)

```text
filter=or(status[eq]:"open",assigneeId[is]:NULL)
```

Notes: Strings and dates are quoted; numbers are not.

## Pagination and ordering

All list endpoints use cursor-based pagination.

- Use **limit** to cap page size (default: 60, max: 60).
- Use **starting_after** to fetch the next page (forward).
- Use **ending_before** to fetch the previous page (backward).
- Responses include **pageInfo** with `hasNextPage`, `startCursor`, and `endCursor`.

Examples:

```bash
# First page
curl -H 'Authorization: Bearer <token>' \
  '<server>/rest/core/companies?limit=60'

# Next page
curl -H 'Authorization: Bearer <token>' \
  '<server>/rest/core/companies?limit=60&starting_after=<endCursorFromPreviousPage>'

# Previous page
curl -H 'Authorization: Bearer <token>' \
  '<server>/rest/core/companies?limit=60&ending_before=<startCursorFromCurrentPage>'
```

You can combine pagination with filters and ordering.

Ordering with `order_by`:
- Shape: `field1,field2[DIRECTION2]`
- Directions: AscNullsFirst, AscNullsLast, DescNullsFirst, DescNullsLast
- Default per-field direction: AscNullsFirst

Examples:
```text
order_by=createdAt
order_by=id[AscNullsFirst],createdAt[DescNullsLast]
```

## Usage with LLMs

You can use AI to generate code based on the OpenAPI schema with the following URLs:

```text
Core: https://twenty.mardu.systems/rest/open-api/core?token=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..tItC59wNx5x6jeF3jAm2405NxIUBhDMjYCIHlX-WYm0
Metadata: https://twenty.mardu.systems/rest/open-api/metadata?token=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..tItC59wNx5x6jeF3jAm2405NxIUBhDMjYCIHlX-WYm0
```

Quick prompt example (Cursor or any agent):

```text
Here is an OpenAPI schema for the Twenty REST API:
https://twenty.mardu.systems/rest/open-api/core?token=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..tItC59wNx5x6jeF3jAm2405NxIUBhDMjYCIHlX-WYm0

Use it to list companies created after 2024-01-01, ordered by createdAt desc, and include only 20 results.
```

Notes:
- Treat the token like a secret; prefer a short-lived Playground token.
- Most editors can fetch and process the schema even if it's large.

 * OpenAPI spec version: v0.1
 */
import type {
  CreateManyNotes201,
  CreateManyNotesParams,
  CreateOneNote201,
  CreateOneNoteParams,
  DeleteManyNotes200,
  DeleteManyNotesParams,
  DeleteOneNote200,
  DeleteOneNoteParams,
  FindManyNotes200,
  FindManyNotesParams,
  FindNoteDuplicates200,
  FindNoteDuplicatesBody,
  FindNoteDuplicatesParams,
  FindOneNote200,
  FindOneNoteParams,
  GroupByNotes200,
  GroupByNotesParams,
  MergeManyNotes200,
  MergeManyNotesBody,
  MergeManyNotesParams,
  Note,
  NoteForUpdate,
  RestoreManyNotes200,
  RestoreManyNotesParams,
  RestoreOneNote200,
  RestoreOneNoteParams,
  UpdateManyNotes200,
  UpdateManyNotesParams,
  UpdateOneNote200,
  UpdateOneNoteParams,
} from '../../model';

/**
 * **order_by**, **filter**, **limit**, **depth**, **starting_after** or **ending_before** can be provided to request your **notes**
 * @summary Find Many notes
 */
export const getFindManyNotesUrl = (params?: FindManyNotesParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/notes?${stringifiedParams}` : `/notes`;
};

export const findManyNotes = async (
  params?: FindManyNotesParams,
  options?: RequestInit,
): Promise<FindManyNotes200> => {
  const res = await fetch(getFindManyNotesUrl(params), {
    ...options,
    method: 'GET',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: FindManyNotes200 = body ? JSON.parse(body) : {};
  return data;
};

/**
 * @summary Create One note
 */
export const getCreateOneNoteUrl = (params?: CreateOneNoteParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/notes?${stringifiedParams}` : `/notes`;
};

export const createOneNote = async (
  note: Note,
  params?: CreateOneNoteParams,
  options?: RequestInit,
): Promise<CreateOneNote201> => {
  const res = await fetch(getCreateOneNoteUrl(params), {
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(note),
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: CreateOneNote201 = body ? JSON.parse(body) : {};
  return data;
};

/**
 * @summary Delete Many notes
 */
export const getDeleteManyNotesUrl = (params?: DeleteManyNotesParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/notes?${stringifiedParams}` : `/notes`;
};

export const deleteManyNotes = async (
  params?: DeleteManyNotesParams,
  options?: RequestInit,
): Promise<DeleteManyNotes200> => {
  const res = await fetch(getDeleteManyNotesUrl(params), {
    ...options,
    method: 'DELETE',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: DeleteManyNotes200 = body ? JSON.parse(body) : {};
  return data;
};

/**
 * @summary Update Many notes
 */
export const getUpdateManyNotesUrl = (params?: UpdateManyNotesParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/notes?${stringifiedParams}` : `/notes`;
};

export const updateManyNotes = async (
  noteForUpdate: NoteForUpdate,
  params?: UpdateManyNotesParams,
  options?: RequestInit,
): Promise<UpdateManyNotes200> => {
  const res = await fetch(getUpdateManyNotesUrl(params), {
    ...options,
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(noteForUpdate),
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: UpdateManyNotes200 = body ? JSON.parse(body) : {};
  return data;
};

/**
 * @summary Create Many notes
 */
export const getCreateManyNotesUrl = (params?: CreateManyNotesParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/batch/notes?${stringifiedParams}` : `/batch/notes`;
};

export const createManyNotes = async (
  note: Note[],
  params?: CreateManyNotesParams,
  options?: RequestInit,
): Promise<CreateManyNotes201> => {
  const res = await fetch(getCreateManyNotesUrl(params), {
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(note),
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: CreateManyNotes201 = body ? JSON.parse(body) : {};
  return data;
};

/**
 * **depth** can be provided to request your **note**
 * @summary Find One note
 */
export const getFindOneNoteUrl = (id: string, params?: FindOneNoteParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/notes/${id}?${stringifiedParams}` : `/notes/${id}`;
};

export const findOneNote = async (
  id: string,
  params?: FindOneNoteParams,
  options?: RequestInit,
): Promise<FindOneNote200> => {
  const res = await fetch(getFindOneNoteUrl(id, params), {
    ...options,
    method: 'GET',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: FindOneNote200 = body ? JSON.parse(body) : {};
  return data;
};

/**
 * @summary Delete One note
 */
export const getDeleteOneNoteUrl = (id: string, params?: DeleteOneNoteParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/notes/${id}?${stringifiedParams}` : `/notes/${id}`;
};

export const deleteOneNote = async (
  id: string,
  params?: DeleteOneNoteParams,
  options?: RequestInit,
): Promise<DeleteOneNote200> => {
  const res = await fetch(getDeleteOneNoteUrl(id, params), {
    ...options,
    method: 'DELETE',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: DeleteOneNote200 = body ? JSON.parse(body) : {};
  return data;
};

/**
 * @summary Update One note
 */
export const getUpdateOneNoteUrl = (id: string, params?: UpdateOneNoteParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/notes/${id}?${stringifiedParams}` : `/notes/${id}`;
};

export const updateOneNote = async (
  id: string,
  noteForUpdate: NoteForUpdate,
  params?: UpdateOneNoteParams,
  options?: RequestInit,
): Promise<UpdateOneNote200> => {
  const res = await fetch(getUpdateOneNoteUrl(id, params), {
    ...options,
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(noteForUpdate),
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: UpdateOneNote200 = body ? JSON.parse(body) : {};
  return data;
};

/**
 * **depth** can be provided to request your **note**
 * @summary Find note Duplicates
 */
export const getFindNoteDuplicatesUrl = (params?: FindNoteDuplicatesParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/notes/duplicates?${stringifiedParams}`
    : `/notes/duplicates`;
};

export const findNoteDuplicates = async (
  findNoteDuplicatesBody: FindNoteDuplicatesBody,
  params?: FindNoteDuplicatesParams,
  options?: RequestInit,
): Promise<FindNoteDuplicates200> => {
  const res = await fetch(getFindNoteDuplicatesUrl(params), {
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(findNoteDuplicatesBody),
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: FindNoteDuplicates200 = body ? JSON.parse(body) : {};
  return data;
};

/**
 * @summary Restore One note
 */
export const getRestoreOneNoteUrl = (id: string, params?: RestoreOneNoteParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/restore/notes/${id}?${stringifiedParams}`
    : `/restore/notes/${id}`;
};

export const restoreOneNote = async (
  id: string,
  params?: RestoreOneNoteParams,
  options?: RequestInit,
): Promise<RestoreOneNote200> => {
  const res = await fetch(getRestoreOneNoteUrl(id, params), {
    ...options,
    method: 'PATCH',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: RestoreOneNote200 = body ? JSON.parse(body) : {};
  return data;
};

/**
 * @summary Restore Many notes
 */
export const getRestoreManyNotesUrl = (params?: RestoreManyNotesParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/restore/notes?${stringifiedParams}` : `/restore/notes`;
};

export const restoreManyNotes = async (
  params?: RestoreManyNotesParams,
  options?: RequestInit,
): Promise<RestoreManyNotes200> => {
  const res = await fetch(getRestoreManyNotesUrl(params), {
    ...options,
    method: 'PATCH',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: RestoreManyNotes200 = body ? JSON.parse(body) : {};
  return data;
};

/**
 * @summary Merge Many notes
 */
export const getMergeManyNotesUrl = (params?: MergeManyNotesParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/notes/merge?${stringifiedParams}` : `/notes/merge`;
};

export const mergeManyNotes = async (
  mergeManyNotesBody: MergeManyNotesBody,
  params?: MergeManyNotesParams,
  options?: RequestInit,
): Promise<MergeManyNotes200> => {
  const res = await fetch(getMergeManyNotesUrl(params), {
    ...options,
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(mergeManyNotesBody),
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: MergeManyNotes200 = body ? JSON.parse(body) : {};
  return data;
};

/**
 * Groups **notes** by specified fields and optionally computes aggregate values for each group.
 * @summary Group By notes
 */
export const getGroupByNotesUrl = (params: GroupByNotesParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/notes/groupBy?${stringifiedParams}` : `/notes/groupBy`;
};

export const groupByNotes = async (
  params: GroupByNotesParams,
  options?: RequestInit,
): Promise<GroupByNotes200> => {
  const res = await fetch(getGroupByNotesUrl(params), {
    ...options,
    method: 'GET',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: GroupByNotes200 = body ? JSON.parse(body) : {};
  return data;
};
